Create config files in this directory to store base info about your sites.

Config files should be in the format [site_name].cfg

Site config names need to match between local and serverdeploy.

These will be ignored by version control.
